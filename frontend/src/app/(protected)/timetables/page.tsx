"use client";
import Link from "next/link";
import { useQuery } from "@tanstack/react-query";
import { useState } from "react";
import { ChevronLeft,ChevronRight,ExternalLink } from "lucide-react";
import { listAcademicTerms,timetableApi } from "@/lib/api";
import { apiErrorMessage } from "@/lib/api-client";
import { EmptyState,ErrorState,LoadingState,PageHeader,StatusBadge } from "@/components/ui";

export default function TimetablesPage(){const[page,setPage]=useState(1);const[term,setTerm]=useState("");const[scope,setScope]=useState("");const[status,setStatus]=useState("");const[search,setSearch]=useState("");const query=useQuery({queryKey:["timetables",page,term,scope,status],queryFn:()=>timetableApi.list({page,page_size:10,academic_term_id:term||undefined,scope_type:scope||undefined,status:status||undefined})});const terms=useQuery({queryKey:["academic-terms"],queryFn:listAcademicTerms});const rows=query.data?.items.filter(x=>x.name.toLowerCase().includes(search.toLowerCase()))??[];return <><PageHeader title="Timetables" description="Browse generated, reviewed, and published timetable records."/><div className="panel mb-5 grid gap-3 p-4 sm:grid-cols-2 xl:grid-cols-5"><input className="field" aria-label="Search timetable name" placeholder="Search name…" value={search} onChange={e=>setSearch(e.target.value)}/><select className="field" aria-label="Academic term" value={term} onChange={e=>{setTerm(e.target.value);setPage(1)}}><option value="">All academic terms</option>{terms.data?.items.map(x=><option key={x.id} value={x.id}>{x.academic_year} {x.term_name}</option>)}</select><select className="field" aria-label="Scope type" value={scope} onChange={e=>{setScope(e.target.value);setPage(1)}}><option value="">All scopes</option>{["COLLEGE","DEPARTMENT","PROGRAM","SECTION"].map(x=><option key={x}>{x}</option>)}</select><select className="field" aria-label="Status" value={status} onChange={e=>{setStatus(e.target.value);setPage(1)}}><option value="">All statuses</option>{["DRAFT","GENERATED","UNDER_REVIEW","APPROVED","PUBLISHED","ARCHIVED"].map(x=><option key={x}>{x}</option>)}</select><button className="button-secondary" onClick={()=>{setTerm("");setScope("");setStatus("");setSearch("");setPage(1)}}>Clear filters</button></div>{query.isLoading?<LoadingState/>:query.isError?<ErrorState message={apiErrorMessage(query.error)} retry={()=>void query.refetch()}/>:rows.length===0?<EmptyState title="No timetables found" detail="Try changing the filters or search term."/>:<div className="panel overflow-x-auto"><table className="w-full text-left text-sm"><thead className="border-b bg-slate-50 text-xs uppercase text-slate-500"><tr><th className="px-4 py-3">Name</th><th className="px-4 py-3">Scope</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Active version</th><th className="px-4 py-3"><span className="sr-only">Actions</span></th></tr></thead><tbody className="divide-y">{rows.map(row=><tr key={row.id} className="hover:bg-slate-50"><td className="px-4 py-4 font-medium">{row.name}</td><td className="px-4 py-4">{row.scope_type}</td><td className="px-4 py-4"><StatusBadge value={row.status}/></td><td className="px-4 py-4">{row.active_version_id?<span className="text-emerald-700">Active</span>:"—"}</td><td className="px-4 py-4 text-right"><Link className="inline-flex items-center gap-1 font-semibold text-brand-700 hover:underline" href={`/timetables/${row.id}`}>Open <ExternalLink className="h-3.5 w-3.5"/></Link></td></tr>)}</tbody></table></div>}<div className="mt-4 flex items-center justify-between text-sm"><span>Page {query.data?.page??page} of {Math.max(query.data?.pages??1,1)} · {query.data?.total??0} records</span><div className="flex gap-2"><button className="button-secondary p-2" aria-label="Previous page" disabled={page<=1} onClick={()=>setPage(x=>x-1)}><ChevronLeft className="h-4 w-4"/></button><button className="button-secondary p-2" aria-label="Next page" disabled={page>=(query.data?.pages??1)} onClick={()=>setPage(x=>x+1)}><ChevronRight className="h-4 w-4"/></button></div></div></>}
